[permissions]
allow = ["Bash=cd \"D:\\Claude Project\" && python -c \"\nfrom PIL import Image\nim = Image.open('dev/shots/flatmap_world.png').convert('RGB')\nprint('size', im.size)\nim2 = im.resize((64, 40))\ncols = im2.getcolors(64*40)\ncols.sort(reverse=True)\nfor cnt, c in cols[:14]:\n    print(cnt, '#%02x%02x%02x' % c, c)\n\"", "Bash=cd \"D:\\Claude Project\" && python -c \"\nfrom PIL import Image\nfor f in ['dev/shots/phase2_biome_s9.png','dev/shots/phase2_height_s7.png']:\n    try:\n        im = Image.open(f).convert('RGB')\n        im2 = im.resize((80, 50))\n        cols = im2.getcolors(80*50)\n        cols.sort(reverse=True)\n        print('==', f, im.size)\n        for cnt, c in cols[:10]:\n            print(' ', cnt, '#%02x%02x%02x' % c)\n    except Exception as e:\n        print(f, 'ERR', e)\n\"", "Bash=cd \"D:\\Claude Project\" && python -c \"\nimport colorsys\nimport re\nsrc = open('app/ui/map_view.py', encoding='utf-8').read()\nm = re.search(r'_BIOME_COLORS = \\{(.*?)\\n\\}', src, re.S)\nimport ast\nd = ast.literal_eval('{' + m.group(1) + '}')\ndef hsv(c):\n    r,g,b = [v/255 for v in c]\n    h,s,v = colorsys.rgb_to_hsv(r,g,b)\n    return (round(h*360,1), round(s,2), round(v,2))\nprint('biome        rgb            hue   sat   val')\nfor k in ['mountain','highland','forest','taiga','jungle','plains','steppe','savannah','coastal','desert','tundra','swamp']:\n    print(f'{k:<12} {d[k]}  {hsv(d[k])}')\nprint()\n# nearest-neighbour hue distance check\nnames = list(d)\ndef hue_d(a,b):\n    ha = hsv(d[a])[0]; hb = hsv(d[b])[0]\n    return min(abs(ha-hb), 360-abs(ha-hb))\nworst = []\nfor a in names:\n    others = sorted(((hue_d(a,b), b) for b in names if b!=a))\n    worst.append((a, others[0][1], round(others[0][0],1)))\nfor a,b,dist in sorted(worst, key=lambda t:-t[2]):\n    print(f'{a:<12} nearest={b:<10} hue-dist={dist}')\n\"", "Bash=cd \"D:\\Claude Project\" && python -c \"\nimport colorsys\nfrom app.ui.map_view import _BIOME_COLORS\nd = _BIOME_COLORS\ndef hsv(c):\n    r,g,b = [v/255 for v in c]\n    h,s,v = colorsys.rgb_to_hsv(r,g,b)\n    return (round(h*360,1), round(s,2), round(v,2))\nprint('biome        rgb            hue    sat  val')\nfor k in d:\n    print(f'{k:<12} {d[k]}  {hsv(d[k])}')\nprint()\nnames = list(d)\ndef hue_d(a,b):\n    ha = hsv(d[a])[0]; hb = hsv(d[b])[0]\n    return min(abs(ha-hb), 360-abs(ha-hb))\nworst = sorted(((hue_d(a,b), a, b) for a in names for b in names if b!=a))\nprint('closest pairs (hue):')\nfor dist,a,b in worst[:6]:\n    print(f'  {a:<10} ~ {b:<10}  {dist:.1f} deg')\n\"", "Bash=cd \"D:\\Claude Project\" && python -c \"\nimport math\ndef d(a,b): return math.dist(a,b)\nOLD = {'mountain':(150,148,150),'highland':(122,126,104),'forest':(40,110,58),'taiga':(46,88,76),'jungle':(26,128,62),'plains':(168,178,84),'steppe':(186,176,116),'savannah':(198,166,78),'coastal':(94,168,176),'desert':(206,178,110),'tundra':(176,184,186),'swamp':(78,96,66)}\nfrom app.ui.map_view import _BIOME_COLORS as NEW\ndef nearest(tbl):\n    worst=[]; names=list(tbl)\n    for a in names:\n        b = min((b for b in names if b!=a), key=lambda b: d(tbl[a],tbl[b]))\n        worst.append((d(tbl[a],tbl[b]),a,b))\n    return sorted(worst)\nprint('OLD palette closest pairs:')\nfor dist,a,b in nearest(OLD)[:5]: print(f'  {a:<10} ~ {b:<10}  {dist:.0f}')\nprint('NEW palette closest pairs:')\nfor dist,a,b in nearest(NEW)[:5]: print(f'  {a:<10} ~ {b:<10}  {dist:.0f}')\no = nearest(OLD); n = nearest(NEW)\nprint(f'mean nearest-neighbor distance: OLD {sum(x[0] for x in o)/12:.0f} -> NEW {sum(x[0] for x in n)/12:.0f}')\n\"", "Bash=cd \"D:\\Claude Project\" && python -c \"\nfrom app.ui.map_view import _terrain_noise, _texture_cell, _BIOME_COLORS, _rgb\n# determinism\nassert _terrain_noise(5, 9, 3) == _terrain_noise(5, 9, 3)\n# texture keeps cells in range and changes most forest cells at full strength\nimport random\nchanged = 0\nfor i in range(2000):\n    x, y = i % 97, i // 97\n    base = _BIOME_COLORS['forest']\n    out = _texture_cell(base, 'forest', x, y, 1.0)\n    assert all(0 <= c <= 255 for c in out), out\n    if out != base: changed += 1\nprint('forest changed', changed, '/2000 cells (expect ~30%)')\n# desert banding fires\nd = sum(1 for i in range(500) if _texture_cell(_BIOME_COLORS['desert'],'desert',i%50,i//50,1.0) != _BIOME_COLORS['desert'])\nprint('desert ripples', d, '/500 (expect ~23%)')\n# political strength is gentler\nstrong = sum(1 for i in range(500) if _texture_cell(_BIOME_COLORS['forest'],'forest',i%50,i//50,1.0) != _BIOME_COLORS['forest'])\nsoft   = sum(1 for i in range(500) if _texture_cell(_BIOME_COLORS['forest'],'forest',i%50,i//50,0.55) != _BIOME_COLORS['forest'])\nprint('forest full', strong, 'vs pol', soft)\nprint('OK')\n\"", "Bash=cd \"D:\\Claude Project\" && python -c \"\nfrom app.ui.map_view import _terrain_noise, _texture_cell, _BIOME_COLORS\nassert _terrain_noise(5, 9, 3) == _terrain_noise(5, 9, 3)\nchanged = sum(1 for i in range(2000) if _texture_cell(_BIOME_COLORS['forest'],'forest',i%97,i//97,1.0) != _BIOME_COLORS['forest'])\nprint('forest changed', changed, '/2000 (expect ~30%)')\nd = sum(1 for i in range(500) if _texture_cell(_BIOME_COLORS['desert'],'desert',i%50,i//50,1.0) != _BIOME_COLORS['desert'])\nprint('desert ripples', d, '/500 (expect ~23%)')\nstrong = sum(1 for i in range(500) if _texture_cell(_BIOME_COLORS['forest'],'forest',i%50,i//50,1.0) != _BIOME_COLORS['forest'])\nsoft   = sum(1 for i in range(500) if _texture_cell(_BIOME_COLORS['forest'],'forest',i%50,i//50,0.55) != _BIOME_COLORS['forest'])\nprint('forest full', strong, 'vs pol-strength', soft)\nprint('OK')\n\"", "Bash=cd \"D:\\Claude Project\" && python -c \"\nfrom app.ui.map_view import _texture_cell, _BIOME_COLORS\nfor name in ['forest','taiga','jungle','swamp','tundra','mountain','highland','steppe','savannah','plains','coastal','desert']:\n    n = 4000\n    base = _BIOME_COLORS[name]\n    full = sum(1 for i in range(n) if _texture_cell(base,name,i%100,i//100,1.0) != base)\n    soft = sum(1 for i in range(n) if _texture_cell(base,name,i%100,i//100,0.55) != base)\n    # intensity check: max channel shift at full strength\n    mx = 0\n    for i in range(n):\n        out = _texture_cell(base,name,i%100,i//100,1.0)\n        mx = max(mx, max(abs(out[j]-base[j]) for j in range(3)))\n    print(f'{name:<10} touched {full/n:6.1%} (pol {soft/n:6.1%})  max shift {mx}')\nprint('OK')\n\"", "Bash=cd \"D:\\Claude Project\" && python - <<'PY'\nfrom app.ui.map_view import _terrain_noise, _texture_cell, _BIOME_COLORS\nassert _terrain_noise(5, 9, 3) == _terrain_noise(5, 9, 3), \"determinism\"\nprint(\"determinism ok\")\nfor name in (\"forest\",\"taiga\",\"jungle\",\"swamp\",\"tundra\",\"mountain\",\n             \"highland\",\"steppe\",\"savannah\",\"plains\",\"coastal\",\"desert\"):\n    n = 4000\n    base = _BIOME_COLORS[name]\n    full = sum(1 for i in range(n)\n               if _texture_cell(base, name, i % 100, i // 100, 1.0) != base)\n    mx = 0\n    for i in range(n):\n        out = _texture_cell(base, name, i % 100, i // 100, 1.0)\n        assert all(0 <= c <= 255 for c in out)\n        mx = max(mx, max(abs(out[j] - base[j]) for j in range(3)))\n    print(f\"{name:<10} touched {full/n:6.1%}  max shift {mx}\")\nprint(\"OK\")\nPY", "Bash=cd \"D:\\Claude Project\" && python -c \"\nimport app.ui.map_view as mv\n# tables exist and agree with each other\nassert set(mv._TERRAIN_SYMBOL_FILL) == set(mv._TERRAIN_SYMBOL_OUTLINE) == set(mv._TERRAIN_GLYPHS)\nfor b in mv._TERRAIN_GLYPHS:\n    assert b in mv._BIOME_COLORS\nassert set(mv._TERRAIN_LEGEND_ORDER) == set(mv._TERRAIN_GLYPHS)\nassert len(mv._TERRAIN_LEGEND_ORDER) == 10\n# every glyph-routed biome has a draw method\nm = mv.MapView\nfor b in mv._TERRAIN_GLYPHS:\n    assert hasattr(m, '_draw_%s_glyph' % b), b\nprint('canvas phase OK:', sorted(mv._TERRAIN_GLYPHS))\n\"", "Bash=cd \"D:\\Claude Project\" && grep -c \"_texture_cell\\|_terrain_noise\" app/ui/map_view.py; echo \"---git status---\"; git status --short 2>/dev/null | head -20; echo \"---python import path---\"; python -c \"import app.ui.map_view as m; print(m.__file__)\"", "Bash=cd \"D:\\Claude Project\" && ls -la --time-style=full-iso app/ui/map_view.py app/ui/__pycache__/map_view*.pyc 2>/dev/null; echo \"---\"; grep -n \"mountain\" app/ui/map_view.py | head -3; echo \"---import with -B (no bytecode)---\"; python -B -c \"import app.ui.map_view as m; print('has _texture_cell:', hasattr(m, '_texture_cell')); print('mountain color:', m._BIOME_COLORS['mountain'])\""]
